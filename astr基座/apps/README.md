# 适配插件目录 (apps/)

此目录出厂为空。请通过基座的 **Web 面板**（后台 → 自定义页面 → "AstrBot 基座"）的
「安装插件」页, 从 GitHub 仓库 URL / `.zip` 直链下载, 或上传本地 zip 安装 AstrBot 插件,
安装后会自动解压到本目录 `apps/<插件名>/` 并热重载基座以注册指令。

也可手动把任意 AstrBot 插件「整个目录」放进这里 (目录内需含 `main.py`)。
